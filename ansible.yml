# - hosts: ec2-dev-k8s
#   tasks:
#     - name: Copiando arquivo de deploy para o diretório do grupo 4
#     - shell: "cp deployment-order-api.yml ./grupo4/"
- hosts: all
  tasks:
    - name: Copiando arquivo de deploy para o diretório do grupo 4
      copy:
      src: 'deployment-order-api.yml'
      dest: '/home/ubuntu/grupo4/deployment-order-api.yml'
      remote_src: yes
